<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ACast"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Db="using:ACast.Database"
    x:Class="ACast.MainPage"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <DataTemplate x:Key="DataTemplate1">
            <Grid Height="100" Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Image x:Name="image" Source="{Binding Image, Mode=OneWay}" Stretch="UniformToFill" Grid.RowSpan="2" VerticalAlignment="Top"/>
                <TextBlock x:Name="titleTextBox" TextWrapping="Wrap" Text="{Binding Title, Mode=OneWay}" VerticalAlignment="Stretch" Grid.Column="1" HorizontalAlignment="Left" Margin="5,0,0,0"/>
                <TextBlock x:Name="infoTextBox" TextWrapping="Wrap" Text="{Binding Info, Mode=OneWay}" VerticalAlignment="Stretch" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Left" Margin="5,0,0,0"/>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="DataTemplate2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock x:Name="textBox" HorizontalAlignment="Stretch" Margin="0" TextWrapping="Wrap" Text="{Binding Title, Mode=TwoWay}" VerticalAlignment="Stretch"/>
                <AppBarButton x:Name="feedItemButton" Grid.Column="1" Click="feedItemButton_Click" Grid.RowSpan="1" Label="{Binding DownloadProgress}">
                    <AppBarButton.Icon>
                        <SymbolIcon Symbol="{Binding MediaItemStateSymbol}"/>
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton x:Name="playItemButton" Grid.Column="2" Click="playItemButton_Click" Grid.RowSpan="2" Icon="Play"/>
            </Grid>
        </DataTemplate>
    </Page.Resources>
    <Page.DataContext>
        <Db:FeedItem/>
    </Page.DataContext>

    <Page.BottomAppBar>
        <CommandBar x:Name="commandBar"/>
    </Page.BottomAppBar>
    <Pivot x:Name="pivot" Title="Pivot">
        <PivotItem x:Name="feedsPivotItem" Header="Feeds">
            <Grid>
                <ListView x:Name="feedListView" IsItemClickEnabled="True" ItemsSource="{Binding Feeds, Mode=OneWay}" ItemTemplate="{StaticResource DataTemplate1}"/>
            </Grid>
        </PivotItem>
        <PivotItem x:Name="feedDetailsPivotItem" Header="Items">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <AutoSuggestBox x:Name="serachFeedDetailsBox" PlaceholderText="Search"/>
                <ListView x:Name="feedItemsListView" IsItemClickEnabled="True" Grid.Row="1" Margin="0,0.333,0,-2.167" ItemTemplate="{StaticResource DataTemplate2}"/>
            </Grid>
        </PivotItem>
        <PivotItem x:Name="playerPivotItem" Header="Player">
            <Grid>
                <local:PlayerControl x:Name="playerControl" Margin="10,0,5,0" HorizontalAlignment="Stretch"/>
            </Grid>
        </PivotItem>
        <PivotItem x:Name="debugPivotItem" Header="Debug">
            <Grid>
                <ListBox x:Name="debugList"/>
            </Grid>
        </PivotItem>
    </Pivot>


</Page>
